# 캔버스

캔버스는 Vineyard 프로젝트가 존재하는 곳입니다: 팬, 줌, 재배치, 주석이 가능한 그래프 위에 배치된 노드와 엣지. 이 페이지에서는 캔버스 내 툴바와 상단 메뉴 바(**프로젝트 / 편집 / 보기**)에서 사용 가능한 모든 컨트롤을 문서화합니다.

## 동일한 작업에 도달하는 두 가지 방법

보기 컨트롤은 두 번 노출되며, 둘 다 **동일한** 실시간 그래프 상태를 구동하므로 항상 동기화됩니다:

- **캔버스 내 툴바** — 캔버스 왼쪽 상단 가장자리에 고정된 컴팩트한 세로 막대로, 작업 중 가장 자주 사용하는 컨트롤이 있습니다.
- **상단 메뉴 바** — `프로젝트`, `편집`, `보기` 메뉴로, 보기 토글과 함께 데이터 및 탐색 작업을 추가합니다.

예를 들어, 메뉴 바에서 그리드를 토글하면 툴바 버튼이 보여주는 것과 동일한 스위치가 전환되며, 그 반대도 마찬가지입니다.

## 보기 컨트롤 (툴바 + 보기 메뉴)

이 컨트롤은 캔버스 내 툴바와 `보기` 메뉴 모두에 나타나며 동일한 상태를 구동합니다. 툴바 토글 버튼은 켜져 있을 때 강조 표시되고, 메뉴 항목은 켜져 있을 때 체크 표시됩니다.

| 컨트롤 | 동작 | 인터페이스 |
| --- | --- | --- |
| **확대** | 캔버스를 확대합니다 (1.2×). | 툴바 |
| **축소** | 캔버스를 축소합니다 (0.8×). | 툴바 |
| **보기 맞춤** | 모든 노드가 보이도록 전체 그래프를 프레임에 맞춥니다. | 툴바, 보기 |
| **레이아웃** | 내장 레이아웃 중 하나를 적용합니다 (아래 참조). 활성 레이아웃에 체크 표시됩니다. | 툴바, 보기 |
| **그리드** | 배경 정렬 그리드를 토글합니다. | 툴바, 보기 |
| **미니맵** | 미니맵 개요 패널을 토글합니다 (우측 하단). | 툴바, 보기 |
| **범례** | 노드 유형 범례를 토글합니다 (좌측 하단). | 툴바, 보기 |
| **스냅라인** | 노드 드래그 시 표시되는 정렬 가이드를 토글합니다. | 툴바, 보기 |
| **패널 레이아웃 초기화** | 프로젝트 작업 공간의 패널 배치를 기본값으로 복원합니다. | 보기 |

!!! note "패널 레이아웃 초기화는 그래프가 아닌 패널을 초기화합니다"
    *패널 레이아웃 초기화*는 주변 작업 공간 패널(캔버스, 사이드 패널 등)을 초기 분할로 복원합니다. 그래프 레이아웃을 다시 실행하거나 노드를 이동하지 **않습니다**. 노드를 재배치하려면 대신 **레이아웃**을 선택하세요.

## 상단 메뉴 바

### 프로젝트

| 항목 | 동작 |
| --- | --- |
| **공유…** | 프로젝트를 보고 편집할 수 있는 사람을 관리하는 공유 대화상자를 엽니다. |
| **그래프 내보내기 (JSON)** | 현재 그래프를 JSON 파일로 다운로드합니다 ([그래프 내보내기](#json-그래프-내보내기) 참조). |
| **마켓플레이스에서 추가…** | 이 프로젝트에 범위가 지정된 [마켓플레이스](../marketplace.md)를 열어 플러그인과 Type Pack을 추가합니다. |
| **프로젝트 목록** | 프로젝트 목록으로 돌아갑니다. |

### 편집

| 항목 | 동작 |
| --- | --- |
| **노드 추가** | 캔버스에 새 노드를 생성합니다. |
| **전체 선택** | 모든 노드를 선택됨으로 표시합니다. |
| **선택 해제** | 모든 노드 선택을 해제하고 선택 개수를 초기화합니다. |

## 레이아웃

툴바의 레이아웃 메뉴와 `보기 ▸ 레이아웃` 하위 메뉴 모두 동일한 세트를 제공합니다. 레이아웃을 적용하면 노드 위치가 재계산된 후 보기가 맞춰집니다.

| 레이아웃 | 설명 |
| --- | --- |
| **Concentric** | 노드를 동심원 고리로 배열하며 겹침을 방지합니다. |
| **Force** | 노드를 밀어내고 연결된 노드를 끌어당기는 힘 기반 레이아웃. |
| **D3 Force** | D3 기반 힘 유도 변형. |
| **Circular** | 노드를 단일 원 주위에 균등하게 배치합니다. |
| **Radial** | 중심 노드에서 바깥쪽으로 노드를 펼칩니다. |
| **Grid** | 노드를 정규 그리드 위에 배치하며 겹침을 방지합니다. |
| **Hierarchical (Dagre)** | 위에서 아래로 계층적 레이아웃, 방향성/트리형 그래프에 적합. |

!!! tip
    낯선 그래프의 시작점으로는 Force와 D3 Force가 가장 좋습니다. 관계가 방향성이고 명확한 계층을 원할 때는 Hierarchical (Dagre)로 전환하세요.

## 오버레이

### 그리드

팬과 줌에 따라 캔버스를 따라다니는 미묘한 배경 그리드로, 노드 정렬을 위한 시각적 참조를 제공합니다.

### 미니맵

우측 하단 모서리에 도킹된 반투명 개요 패널. 현재 보기를 표시하는 뷰포트 직사각형과 함께 전체 그래프를 축소판으로 보여줍니다 — 대규모 프로젝트에서 방향을 잡는 데 유용합니다.

### 범례

좌측 하단의 반투명 미니맵 스타일 패널로, 그래프에 **현재 존재하는** 노드 유형을 나열합니다. 각 항목은 노드의 유형 색상과 일치하는 색상 견본, 유형의 표시 이름, 해당 유형의 노드 개수를 **표시**합니다. 항목은 레이블 기준 알파벳 순으로 정렬됩니다. 그래프가 비어 있으면 범례에 "노드 없음"이라고 표시됩니다.

범례는 프로젝트가 사용하는 Type Pack을 반영합니다 — 예를 들어, [Infrastructure Type Pack](typepacks.md)으로 구축된 조사는 `infrastructure.ip_address`, `infrastructure.domain` 등의 개수를 표시하며, 각각 Type Pack에서 정의된 레이블과 색상으로 해석됩니다.

### 스냅라인

활성화된 경우, 노드를 다른 노드의 가장자리나 중심 근처로 드래그하면 정렬 가이드(수직 및 수평선)가 표시되고 노드가 정렬 위치에 스냅됩니다. 그리드, 미니맵, 범례와 달리 스냅라인은 지속적 패널이 없습니다 — 활발히 드래그하는 동안에만 나타납니다.

## JSON 그래프 내보내기 {#json-그래프-내보내기}

`프로젝트 ▸ 그래프 내보내기 (JSON)`은 프로젝트의 현재 노드와 엣지를 프로젝트 이름을 딴 JSON 파일로 다운로드합니다. 내보내기는 그래프 데이터의 스냅샷을 캡처합니다:

```json
{
  "project": "my-investigation",
  "exported_at": "2026-06-28T12:00:00.000Z",
  "nodes": [ /* … */ ],
  "edges": [ /* … */ ]
}
```

## 마켓플레이스에서 추가

`프로젝트 ▸ 마켓플레이스에서 추가…`는 현재 프로젝트에 범위가 지정된 [마켓플레이스](../marketplace.md)를 열어, 프로젝트에 직접 연결된 플러그인과 Type Pack을 추가할 수 있습니다. 전체 흐름은 [설치하기](installing.md)를 참조하세요.

## 다음 / 함께 보기

- [플러그인 실행하기](running-plugins.md) — 캔버스의 노드에 대해 플러그인을 실행합니다.
- [Type Pack](typepacks.md) — 노드 유형, 색상, 레이블이 범례를 구동하는 방식.
- [작업](tasks.md) — 장기 실행 플러그인 실행을 추적합니다.
- [마켓플레이스 둘러보기](../marketplace.md) — 추가할 팩을 찾습니다.
